import { PictureHoverDirective } from './picture-hover.directive';

describe('PictureHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PictureHoverDirective();
    expect(directive).toBeTruthy();
  });
});
